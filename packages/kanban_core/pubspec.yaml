name: kanban_core
description: >-
  Round-trip-safe parser, serializer and editing operations for Obsidian
  Kanban-plugin markdown boards. Pure Dart, no Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
